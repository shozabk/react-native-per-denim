import {create} from 'apisauce';

export const apiClient = create({
  baseURL: 'https://jsonplaceholder.typicode.com/',
});

export const webClientId = ''; //ADD WEBCLIENT ID FROM GOOGLE OAUTH
export const iosClientId = ''; // ADD IOS CLIENT ID FROM GOOGLE OAUTH
