import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  logo: {
    fontSize: 50,
    fontWeight: 'bold',
    alignSelf: 'center',
    marginTop: '50%',
  },
});

export default styles;
